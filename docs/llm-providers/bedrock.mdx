---
title: "Amazon Bedrock"
description: "Bedrock requires a ProviderAdapter v2 plugin"
---

Amazon Bedrock uses the AWS credential chain, regions, and provider-specific model identifiers. Install a trusted `strix.providers` plugin that implements those native contracts. Bedrock is not exposed through the Custom OpenAI-compatible form.
