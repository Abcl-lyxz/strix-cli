---
title: "OpenRouter"
description: "Connect OpenRouter and discover models"
---

Run `/connect`, choose **OpenRouter**, and enter the API key. `/models` uses the adapter's model endpoint and retains exact provider model IDs. Enable multiple compatible models when you want the router to balance quality, reliability, cost, and latency.
