---
title: "Anthropic"
description: "Connect Anthropic through the provider-native TUI adapter"
---

Run `/connect`, choose **Anthropic**, and enter the API key. Open `/models` to inspect catalog metadata, capability verification, and enabled models.

An `ANTHROPIC_API_KEY` environment value is a read-only detection hint, not configuration precedence.
